const initialState = {
  isAuthenticated: false,
  user: {},
};

const authReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    case 'value':
      break;

    default:
      return state;
  }
};
